{% extends 'base.html.twig' %}

{% block title %}Order2{% endblock %}

{% block body %}
    <h1>Order2</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order2.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ order2.date ? order2.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Volum</th>
                <td>{{ order2.volum }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_order2_index') }}">back to list</a>

    <a href="{{ path('app_order2_edit', {'id': order2.id}) }}">edit</a>

    {{ include('order2/_delete_form.html.twig') }}
    <button type="button" class="btn btn-success"><a href="{{ path('app_home') }}">Accueil</a></button>

{% endblock %}
